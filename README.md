# diff-buddy
A PowerShell 7+ utility that compares two drives or folders — scanning both paths in parallel, hashing every file, checking every path, and producing a clean CSV of differences. Designed for verifying drive migrations, backups, and large file copies at scale. The script and documentation were written with for speed.
